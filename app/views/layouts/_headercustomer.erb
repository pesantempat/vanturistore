<head>
  <meta charset="utf-8">
  <title>VanturiStore</title>
  <!-- Tell the browser to be responsive to screen width -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    .nav-link:hover {
  text-decoration: underline 2px solid #07a99c;
  }
  .card-header {
    background-color: #07a99c !important;
    color: #fff;
    font-weight: 700;  
  }

  .card-title {
    float: left;
    font-size: 1.1rem;
    font-weight: 700;
    margin: 0;
  }

  .content-header {
    padding: 0px .5rem;
  }

  label:not(.form-check-label):not(.custom-file-label) {
    font-weight: 500;
    color: #07a99c;
  }

  .submit-btn {
  color: #fff;
  background-color: #07a99c;
  font-weight: 700;
  height: 40px;
  padding: 10px 35px;
  font-size: 14px;
  border: none;
  display: block;
  width: 100%;
  text-transform: capitalize;
}

 .submit-btn:hover {
  background-color: #0ac0b1;
}

.ui-widget-header {
    border: 1px solid #07a99c !important;
    background: #07a99c !important;
    }
  </style>
  
</head>
<body class="hold-transition sidebar-mini layout-fixed">
<div class="wrapper">

  <!-- Navbar -->
  <nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
      </li>
    </ul>
    
    </form>
      <a href="<%= edit_customer_registration_path %>" class="nav-link" style="color:#07a99c; font-weight: 700;">Setting</a>
      <a href="<%=destroy_customer_session_path%>" rel="nofollow" data-method="delete" class="nav-link" style="margin-left:-20px; color:#07a99c; font-weight: 700;">Log Out</a>  
  </nav>
  <!-- /.navbar -->
    <!-- Main Sidebar Container -->
  <aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a href="<%=homecustomer_path%>" class="brand-link">
      <%= image_tag 'favicon.ico', :class =>"brand-image img-circle elevation-3", :style =>"opacity: .8" %>     
      <span class="brand-text font-weight-light">VanturiStore</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">
      <!-- Sidebar user panel (optional) -->
      <div class="user-panel mt-3 pb-3 mb-3 d-flex">  
        <div class="image">
          <%= image_tag 'avatar5.png', :class =>"img-circle elevation-2"%>
        </div>
        <div class="info">
          <a href="<%=homecustomer_path%>" class="d-block"><%= current_customer.name_customer %></a>
        </div> 
      </div>

      <!-- Sidebar Menu -->
      <nav class="mt-2">
        <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
          <!-- Add icons to the links using the .nav-icon class
               with font-awesome or any other icon font library -->
          <li class="nav-item">
            <a href="<%=homecustomer_path%>" class="nav-link active" style="background-color:#07a99c;>
              <i class="nav-icon fas fa-tachometer-alt"></i>
              <p>
                Dashboard
              </p>
            </a>
          </li>       

          <li class="nav-header">DATA LOYALTY</li>

          <li class="nav-item">
            <% @t_mitras.each do |t_mitra| %>
              <% if loyalty_point = @loyalty_points.find_by(customer_id: current_customer.id, t_mitra_id: t_mitra.id) %>
              <a href="<%=customer_mypoint_path(current_customer.id)%>" class="nav-link">
                <i class="nav-icon fas fa-coins"></i>
                <p>
                  My Loyalty Point
                </p>
              </a>
              <%end%>
            <%end%>
          </li>

          
          <!--<li class="nav-item">
            <a href="#" class="nav-link">
              <i class="nav-icon fas fa-file"></i>
              <p>
                 My Profil
              </p>
            </a>
          </li>-->  

        </ul>
      </nav>
      <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
    
  </aside>
